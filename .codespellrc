[codespell]
skip = .git,pnpm-lock.yaml,node_modules,**/fixtures/**
# some modules, parts of regexes, and variable names to ignore, some
# misspellings in fixtures/external responses we do not own
ignore-words-list = NWO,kernal
